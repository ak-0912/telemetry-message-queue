# telemetry-message-queue
A custom, self-contained message queue built in Go, exposed over grpc, deployable on Kubernetes. It connects Telemetry Streamer (producers) with Telemetry Collectors (consumers).
